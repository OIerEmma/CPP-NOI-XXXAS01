//
// Created by Geek.Kwok on 2026/6/27.
//
#include<bits/stdc++.h>
using namespace std;

const int INF = 1e9;
const int MOD = 1e9 + 7;
const int N = 1e5;
vector<int> a(N);
vector<long long> dp(N + 1, 0);
vector<int> ch[N];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // 读入数据
    int n;
    cin >> n;
    // 核心算法

    // 输出结果
    return 0;
}